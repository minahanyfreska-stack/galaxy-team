Galaxy Teams - Netlify static version
This version runs entirely in the browser so it can open on Netlify without a Node.js server.
Data (members, posts, scores) is stored in each browser's localStorage, so changes are NOT shared between different devices.
Admin password: Galaxy2026
For shared multi-device data, connect the app to a hosted database/backend later.
